A little express app that calls the [status server][status-server] and renders with express-handlebars

[status-server]: https://github.com/composerinteralia/status-server

```sh
yarn install
yarn start
open http://localhost:8000
```
